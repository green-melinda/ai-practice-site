# Later

Post-launch ideas land here instead of in the build. Launch scope is closed; if it feels urgent, it still goes here.

---

- 2026-07-07 — Dark mode. Civic Ink v1.4 defines a full night palette; out of launch scope per the brief. Transcribing it into `tokens.css` is a 10-minute job post-launch, plus a contrast pass.
- 2026-07-08 — find-a-film design system. Wanted mid-build on launch week; per the risk table this is "fix one thing in the toy first" at scale. Compromise shipped: tokens-only extraction (`PROJECTS/find-a-film/tokens.css`, v0.1 — transcription + audit, 30-min timebox). Full system (spacing grid, type consolidation, chosen typeface, components) is post-launch, and a strong candidate for process essay #3. It stays deliberately distinct from Civic Ink — the toy keeps its own voice.
